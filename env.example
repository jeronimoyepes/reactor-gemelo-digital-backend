# Database Configuration
DB_PATH=users.db
DB_TYPE=sqlite

# Experiments TSV tables
UPLOADS_DIR=uploads

# File Upload Configuration (in MB)
UPLOAD_MEMORY_LIMIT_MB=100
UPLOAD_MAX_FILE_SIZE_MB=50
UPLOAD_MAX_BODY_SIZE_MB=200
UPLOAD_TEMP_DIR=/tmp

# Server Configuration
HOST=0.0.0.0
PORT=8080
DEBUG=true
RELOADER=true

# Authentication Settings
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123
SESSION_EXPIRY_HOURS=24

# Security Settings
TOKEN_LENGTH=32
PASSWORD_HASH_ALGORITHM=sha256

# Application Settings
APP_NAME=Reactor Backend API
APP_VERSION=1.0.0 